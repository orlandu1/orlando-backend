# Exemplo de variáveis de ambiente (NÃO comitar segredos)

# Banco
DATABASE_URL=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=1d

# Email (Brevo SMTP)
EMAIL_ENABLED=true
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=
BREVO_SMTP_PASS=
EMAIL_FROM=
EMAIL_FROM_NAME=
EMAIL_DEBUG=false

# Vercel / Blob (se aplicável)
BLOB_READ_WRITE_TOKEN=
VERCEL_OIDC_TOKEN=
